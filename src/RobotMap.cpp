#include "RobotMap.h"

// ==========================================================================

constexpr int MOTOR_CAN_ID = 1;

// ==========================================================================

std::shared_ptr<CANTalon> RobotMap::motor;

// ==========================================================================

void RobotMap::Initialize() {
	motor.reset(new CANTalon(MOTOR_CAN_ID));
}

// ==========================================================================
